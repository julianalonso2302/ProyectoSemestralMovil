export class Cancha {
  id_cancha!: number;       // ID de la cancha, que será la clave primaria
  tipo_deporte!: string;    // Tipo de deporte (fútbol, pádel, etc.)
  nombre_cancha!: string;   // Nombre de la cancha
  estado_cancha!: string;   // Estado de la cancha (disponible, ocupada, etc.)
}
